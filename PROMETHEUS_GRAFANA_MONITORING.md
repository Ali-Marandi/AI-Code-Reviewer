# معماری مانیتورینگ خودکار لایسنس‌ها با Prometheus و Grafana

این سند نحوه پیاده‌سازی زیرساخت مانیتورینگ بلادرنگ برای پایش سلامت سرور لایسنس، وضعیت فعال‌سازی صندلی‌ها و ارزیابی تله‌متری سیستم در نرم‌افزار **AI-Code-Reviewer Enterprise** را تشریح می‌کند.

---

## ۱. معماری جمع‌آوری متریک‌ها (Metrics Collection Architecture)

برای نظارت مداوم بر عملکرد سرور لایسنس و سرویس‌های پشتیبان، بک‌اند FastAPI با کتابخانه `prometheus-fastapi-instrumentator` مجهز شده است تا متریک‌های حیاتی را اکسپوز کند:

- **متریک‌های کلیدی قابل رصد:**
  - `http_requests_total`: تعداد کل درخواست‌های اعتبارسنجی لایسنس و وب‌هوک‌ها.
  - `active_licenses_gauge`: تعداد کل پروانه‌های فعال و صندلی‌های اشغال‌شده (Seats) در لحظه.
  - `license_validation_latency_seconds`: زمان پاسخگویی سرور لایسنس در درخواست‌های گره‌گذاری سخت‌افزاری.
  - `database_connection_pool_status`: وضعیت اتصالات پایگاه داده محلی و ابری.

## ۲. پیکربندی داشبورد در گرافانا (Grafana Dashboards)
با اتصال Prometheus به گرافانا، داشبورد اختصاصی سازمانی با پنل‌های زیر راه‌اندازی می‌شود:
- **پنل وضعیت سلامت سیستم (Health Status):** نمایشگر سبز/قرمز وضعیت آپ‌تایم سرور لایسنس و پایگاه داده.
- **پنل مصرف صندلی‌ها (Seat Utilization Trend):** نمودار خطی روند رشد فعال‌سازی لایسنس‌ها در سازمان‌های مختلف.
- **هشداردهی خودکار (Alerting Rules):** ارسال هشدارهای فوری از طریق Webhook به کانال Slack یا ایمیل تیم DevOps در صورت افزایش نرخ خطاهای اعتبارسنجی لایسنس یا تاخیر پاسخگویی سرور بیش از ۵۰۰ میلی‌ثانیه.
