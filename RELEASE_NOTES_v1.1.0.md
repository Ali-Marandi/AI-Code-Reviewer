# یادداشت انتشار — v1.1.0

**AI-Code-Reviewer Enterprise**
**نوع انتشار:** قابلیت جهانی‌سازی و آماده‌سازی محلی‌سازی
**وضعیت:** آماده انتشار کنترل‌شده پس از CI ویندوز

## خلاصه

v1.1.0 اولین پایه محلی‌سازی قابل‌اعتماد محصول را عرضه می‌کند. رابط دسکتاپ اکنون در شروع برنامه `--locale auto|en|fa` می‌پذیرد، Catalog فارسی بسته‌بندی‌شده را به‌صورت آفلاین بارگذاری می‌کند و برای فارسی جهت راست‌به‌چپ اعمال می‌شود. این نسخه کامل‌بودن ترجمه همه عناصر یا آمادگی عرضه در هر کشور را ادعا نمی‌کند؛ هدف آن ایجاد زنجیره قابل‌ممیزی برای توسعه مرحله‌ای localeهای بعدی است.

## تغییرات

| حوزه | تغییر | کنترل امنیتی/کیفی |
|---|---|---|
| انتخاب locale | گزینه `--locale auto|en|fa` در حالت GUI | ورودی locale به allowlist محدود است؛ مقدار ناشناخته به انگلیسی برمی‌گردد. |
| بارگذاری Catalog | ماژول `core/i18n.py` از `QTranslator` استفاده می‌کند. | فقط Catalog همراه برنامه و از مسیر bundle بارگذاری می‌شود؛ URL و فایل انتخابی کاربر پذیرفته نمی‌شود. |
| فارسی و RTL | Catalog فارسی اولیه و جهت راست‌به‌چپ برای locale `fa` | Smoke Test در حالت offscreen عنوان فارسی و RTL را بررسی می‌کند. |
| زنجیره ساخت | Catalogهای `translations/` به build PyInstaller ویندوز افزوده شدند. | CI نسخه QM را به‌عنوان Asset محلی همراه EXE بسته‌بندی می‌کند. |
| آزمون | تست انتخاب امن locale و Smoke Test UI گسترش یافت. | locale نامعتبر و الگوهای مسیر غیرقابل‌اعتماد به fallback انگلیسی می‌روند. |

## اجرا

```bash
python app.py --locale fa
python app.py --locale en
python app.py --locale auto
```

## مرزهای محصول

Qt چرخه استاندارد محلی‌سازی را بر پایه رشته‌های قابل‌ترجمه، TS، ابزارهای extraction/release و فایل‌های runtime تعریف می‌کند.[1] مستندات `QTranslator` همچنین تأکید می‌کند که فایل‌های ترجمه باید فقط از منابع مورداعتماد نصب شوند.[2] در نتیجه، v1.1.0 برای Catalogهای خارجی، دانلود runtime ترجمه یا دریافت فایل ترجمه از کاربر API ارائه نمی‌کند.

> ترجمه فارسی اولیه تنها نقطه آغاز QA محلی‌سازی است. متن قرارداد، اخطار حقوقی، قیمت، سیاست حفظ حریم خصوصی و ادعاهای انطباق برای هر بازار باید توسط مالک محلی و مشاور حقوقی واجدصلاحیت تأیید شوند.

## کنترل‌های پذیرش

| کنترل | نتیجه پیش از انتشار |
|---|---|
| Compile Catalog فارسی | موفق؛ ۳۰ رشته نهایی تولید شد. |
| Syntax check برنامه، UI و i18n | موفق |
| Unit testها | ۸ از ۸ موفق |
| Smoke Test انگلیسی و فارسی در حالت offscreen | موفق |
| کنترل whitespace | موفق |

## منابع

[1]: https://doc.qt.io/qt-6/localization.html "Qt 6 — Localizing Applications"
[2]: https://doc.qt.io/qtforpython-6/PySide6/QtCore/QTranslator.html "Qt for Python — QTranslator"
