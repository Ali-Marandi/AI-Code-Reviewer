# یادداشت انتشار — v1.2.0 (Commercial Advanced Features)

**AI-Code-Reviewer Enterprise**  
**نوع انتشار:** ارتقای تجاری پیشرفته (Advanced Enterprise Commercial Release)  
**وضعیت:** تأییدشده با تست‌های واحد موفق (`10/10` آزمون موفق)

## خلاصه

نسخه **v1.2.0** قابلیت‌های تجاری اینترپرایز محصول را به سطح رقابتی پیشرفته‌ترین ابزارهای بازبینی کد بازار ارتقا می‌دهد. این نسخه دو ماژیل قدرتمند جدید را بدون نقض معماری Local-first معرفی می‌کند:
1. **موتور تولید پچ اصلاح خودکار (Automated Refactoring Diff Generator):** قابلیت تبدیل خودکار الگوهای آسیب‌پذیر رایج (مانند کلیدهای سخت‌کدشده یا توابع ناامن `eval`) به کدهای استاندارد امن (جستجوی متغیر در محیط یا `ast.literal_eval`) به همراه تولید Unified Diff قابل بازبینی.
2. **موتور معافیت سیاست‌های سازمانی (Custom Policy Exemption Engine):** امکان تعریف فایل معافیت‌های نسخه‌دار (`policy_exemptions.json`) برای مستثنی کردن الگوها یا فایل‌های خاص (مانند تست فیکسچرها) از هشدارهای سخت‌گیرانه SAST.

## فایل‌های اضافه شده
- `core/ai_engine_v120_features.py`: هسته پردازش اصلاح و معافیت‌های سیاستی.
- `rules/policy_exemptions.json`: کاتالوگ پیش‌فرض معافیت‌های سازمانی.
- `tests/test_v120_features.py`: مجموعه آزمون‌های واحد تضمین کیفیت.

## نتایج آزمون
تمام ۱۰ آزمون واحد سیستم (شامل تحلیل‌های قبلی، i18n و قابلیت‌های جدید v1.2.0) با موفقیت کامل اجرا شدند.
