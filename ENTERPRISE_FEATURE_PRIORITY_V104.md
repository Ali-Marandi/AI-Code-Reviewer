# ارزیابی و اولویت‌بندی قابلیت‌های پیشرفته Enterprise — v1.0.4

**محصول:** AI-Code-Reviewer Enterprise
**هدف:** تبدیل موتور Local-first فعلی به ابزار قابل ادغام در فرایندهای سازمانی، با حفظ کنترل انسانی، قابلیت ممیزی و حداقل‌سازی داده.

## جمع‌بندی اجرایی

نسخه فعلی پروژه در هسته خود Local SAST مبتنی بر Regex/AST، تحلیل وابستگی، پیش‌بینی ساده باگ و Cache SQLite دارد؛ در رابط کاربری نیز Pull Request، جدول یافته‌ها و گزارش GitHub پشتیبانی می‌شود. فاصله مهم تا نیاز Enterprise، نه صرفاً «قابلیت AI بیشتر»، بلکه **قابلیت اعمال سیاست سازمانی، تبادل یافته با ابزارهای موجود، و مشاهده روند ریسک در طول زمان** است.

برای v1.0.4 سه قابلیت به‌صورت یکپارچه اولویت اول گرفته‌اند: **Custom Rule Engine محلی و امن، خروجی استاندارد SARIF 2.1.0، و ثبت/نمایش روند یافته‌ها در SQLite.** این مجموعه با کد فعلی هم‌راستا است، نیاز به سرویس ابری جدید ندارد و مسیر ادغام با GitHub Code Scanning یا CI سازمانی را باز می‌کند.[1] [2]

| اولویت | قابلیت | تصمیم | دلیل تجاری/فنی |
|---|---|---|---|
| P0 | Custom Rule Engine امن | پیاده‌سازی در v1.0.4 | سازمان‌ها باید بتوانند سیاست‌های داخلی را بدون اجرای کد یا Plugin ناامن اعمال کنند. |
| P0 | SARIF 2.1.0 Export | پیاده‌سازی در v1.0.4 | یافته‌های Local-first را به فرمت استاندارد قابل مصرف در GitHub/CI تبدیل می‌کند.[1] [2] |
| P0 | Historical Findings & Trend | پیاده‌سازی در v1.0.4 | گفتگو با مدیر فنی را از «لیست یافته امروز» به «روند ریسک و اثر اصلاح» تغییر می‌دهد. |
| P1 | Multi-language rule packs برای Java/Go/Rust | پیاده‌سازی محدود در v1.0.4 | UI از قبل فایل‌های Java/Go/Rust را بررسی می‌کند؛ Rule Engine می‌تواند قوانین قابل‌تست برای آنها اجرا کند. |
| P1 | Baseline/Waiver با انقضا و دلیل | طراحی برای v1.0.5 | برای کاهش Noise ضروری است، اما بدون مدل هویت، مسیر تصویب و Audit UI کامل، اجرای شتاب‌زده ریسک ایجاد می‌کند. |
| P1 | Policy pack signing و کنترل نسخه | طراحی برای v1.0.5 | برای توزیع قواعد سازمانی مفید است، اما ابتدا باید schema، تست و چرخه به‌روزرسانی پایدار شوند. |
| P2 | Integration با Jira/Linear | طراحی/بررسی در فاز بعد | نیازمند OAuth، مالکیت داده، مدل Issue Mapping و احتمالاً سرویس سمت سرور است. |
| P2 | Refactoring خودکار چندفایلی | تحقیق جداگانه | تغییر خودکار کد بدون parser چندزبانه، تست و Diff/rollback کامل، ریسک محصولی بالایی دارد. |
| P2 | همکاری هم‌زمان و فضای ابری مشترک | پس از طراحی کنترل دسترسی | به Backend، SSO، retention policy و threat model مستقل نیاز دارد. |

## روش امتیازدهی

امتیازها برای انتخاب داخلی‌اند و ادعای داده بازار نیستند. هر معیار از ۱ (کم) تا ۵ (زیاد) امتیاز دارد. امتیاز بالاتر در «ریسک اجرا» به معنی دشوارتر/پرریسک‌تر بودن است؛ بنابراین در محاسبه اولویت، از وزن منفی استفاده شده است.

```text
Priority Score = (0.30 × Enterprise Value) + (0.25 × Customer Pull) +
                 (0.20 × Architectural Fit) + (0.15 × Evidence/Compliance Value) −
                 (0.10 × Execution Risk)
```

| قابلیت | ارزش Enterprise | کشش مشتری | هم‌خوانی معماری | شواهد/Compliance | ریسک اجرا | امتیاز تصمیم | نتیجه |
|---|---:|---:|---:|---:|---:|---:|---|
| Rule Engine محلی | 5 | 5 | 5 | 4 | 2 | 4.40 | P0 |
| SARIF Export | 5 | 4 | 5 | 5 | 2 | 4.35 | P0 |
| روند فنی/یافته‌ها | 4 | 4 | 5 | 4 | 2 | 3.90 | P0 |
| Rule Pack چندزبانه | 4 | 4 | 4 | 3 | 3 | 3.45 | P1، دامنه محدود |
| Waiver/Baseline | 4 | 4 | 3 | 5 | 4 | 3.40 | طراحی برای v1.0.5 |
| Jira/Linear Integration | 4 | 4 | 2 | 3 | 4 | 2.80 | طراحی بعدی |
| Refactoring خودکار چندفایلی | 5 | 4 | 2 | 3 | 5 | 2.55 | تحقیق جداگانه |
| همکاری ابری | 5 | 4 | 1 | 4 | 5 | 2.35 | پس از Backend/SSO |

## قابلیت P0-1: Custom Rule Engine محلی و امن

قواعد سفارشی، روش استانداردی برای تبدیل سیاست‌های Secure Coding و کنترل‌های اختصاصی سازمان به اسکن قابل تکرار هستند. به عنوان یک شاهد صنعتی، Semgrep قواعد سفارشی را برای شناسایی مشکلات امنیتی، نقض‌های Secure Coding، خطاها و حتی فایل‌های پیکربندی معرفی می‌کند.[3] این پروژه، به‌جای اجرای زبان قواعد یا کد افزونه، نسخه اولیه‌ای محدود و ایمن می‌سازد که فقط **JSON و Regex محدود** را می‌پذیرد.

| مولفه | تصمیم v1.0.4 | کنترل امنیتی |
|---|---|---|
| فرمت Rule Pack | فایل JSON با `schema_version` و آرایه `rules` | فایل ناشناخته یا ساختار نادرست رد می‌شود. |
| قابلیت هر Rule | `id`، `pattern`، `languages`، `severity`، `category`، `description`، `suggestion` | هیچ `eval`، اسکریپت، قالب پویا یا اجرای subprocess وجود ندارد. |
| Regex | Python `re` با مدیریت خطا | Regex نامعتبر کل اسکن را متوقف نمی‌کند و به‌صورت Notice ثبت می‌شود. |
| دامنه زبان | پسوندهای پشتیبانی‌شده از Python، JS/TS، Java، Go و Rust | برچسب زبان باید صریح باشد؛ هیچ تشخیص ادعایی parser عمیق چندزبانه نمی‌شود. |
| تغییرپذیری | Rule Pack پیش‌فرض پروژه + انتخاب فایل محلی توسط کاربر | نسخه/مسیر Rule Pack در نتیجه و SARIF ثبت می‌شود. |

## قابلیت P0-2: خروجی SARIF 2.1.0

SARIF یک استاندارد OASIS برای خروجی ابزارهای تحلیل ایستا است.[1] GitHub اجازه می‌دهد فایل SARIF تولیدشده توسط ابزار یا CI خارجی بارگذاری شود تا Alertهای ابزار ثالث در مخزن دیده شوند.[2] بنابراین، خروجی SARIF به مشتری اجازه می‌دهد بدون ارسال کد به سرویس جدید، یافته‌های AI-Code-Reviewer را به فرایندهای موجود خود وارد کند.

| عنصر خروجی | نگاشت از مدل فعلی | دلیل |
|---|---|---|
| `tool.driver` | AI-Code-Reviewer Enterprise و نسخه | شناسایی منبع یافته در ابزار پایین‌دستی |
| `rules[]` | Rule ID، توضیح و سطح شدت | امکان Triage و یکپارچگی قابل فهم |
| `results[]` | یافته‌ها، متن، فایل، خط شروع و fingerprint | محل‌یابی و جلوگیری بهتر از تکرار |
| `runAutomationDetails.id` | دسته تحلیل/Rule Pack | آماده‌سازی برای تحلیل‌های چندگانه |
| `properties` | دسته، پیشنهاد، زبان، منبع محلی/AI | حفظ زمینه برای ابزارهای داخلی |

> SARIF Export در v1.0.4 یک **خروجی فایل محلی** است. بارگذاری در GitHub، ارسال به CI یا تغییر وضعیت Alertها یک عمل بیرونی و نیازمند مجوز کاربر/پیکربندی جداگانه خواهد بود.

## قابلیت P0-3: Historical Findings و روند بدهی فنی

ثبت خلاصه اسکن و fingerprint یافته‌ها در SQLite، سه تصمیم مدیریتی را ممکن می‌کند: تشخیص افزایش یافته‌های High/Critical، سنجش روند Fix بعد از Release، و مشاهده حجم بررسی بدون ذخیره کد یا Secret. در v1.0.4، فقط metadata اسکن، شمارش یافته و fingerprint هش‌شده ذخیره می‌شود؛ **کد منبع، Patch، Token یا متن کامل یافته‌ها در تاریخچه ذخیره نمی‌شود.**

| داده ذخیره‌شده | هدف | دلیل حریم خصوصی |
|---|---|---|
| زمان اسکن، نام/هش مسیر، زبان | تحلیل روند در طول زمان | به جای متن فایل/کد استفاده می‌شود. |
| Severity و Category | روند ریسک و بدهی فنی | داده کمینه برای داشبورد است. |
| fingerprint هش‌شده | تشخیص مشاهده مجدد | از نمایش snippet یا داده محرمانه جلوگیری می‌کند. |
| شناسه Rule و منبع Rule Pack | ممیزی سیاست فعال | امکان مقایسه تغییر Ruleها بدون ذخیره کد. |

## قابلیت P1: Rule Pack چندزبانه با دامنه کنترل‌شده

پشتیبانی چندزبانه در v1.0.4 معادل «تحلیل نحوی عمیق برای Java/Go/Rust» نیست. دامنه متعهدشده، اجرای Ruleهای Regex قابل تست روی فایل‌های دارای پسوند شناخته‌شده و افزودن metadata زبان به یافته‌ها است. این انتخاب، ارزش سریع برای سیاست‌های سازمانی مثل منع Secret، اجرای ناامن فرآیند، یا الگوهای خطرناک متداول فراهم می‌کند، بدون اینکه دقت parserهای رسمی را به‌صورت نادرست ادعا کند.

## مواردی که عمداً در v1.0.4 پیاده‌سازی نمی‌شوند

| قابلیت | علت تعویق | پیش‌نیاز تصمیم بعدی |
|---|---|---|
| Waiver / Suppression | نیازمند نقش کاربر، دلیل، تاریخ انقضا و Audit قابل نمایش است. | مدل هویت، UI Triage و سیاست نگهداشت |
| امضای Rule Pack | نیازمند کلیدها، Rotation و Trust Store است. | طراحی مدیریت کلید و فرآیند انتشار Pack |
| Issue tracking Integration | نیازمند OAuth/Token Scope، نگاشت Issue و کنترل حریم خصوصی است. | Threat model و Backend/connector plan |
| Refactor خودکار | نیازمند AST/CST چندزبانه، Diff/Preview، تست و rollback است. | Design جداگانه برای کنترل انسانی |

## تعریف موفقیت Release v1.0.4

| معیار پذیرش | روش تأیید |
|---|---|
| Rule Pack معتبر، یافته قابل تکرار تولید کند. | آزمون واحد با Python، Java، Go و Rust نمونه. |
| Rule Pack نامعتبر یا Regex خراب اسکن را متوقف نکند. | آزمون واحد خطا و پیام Notice. |
| SARIF حاصل با JSON Parser معتبر باشد و `version=2.1.0` داشته باشد. | آزمون واحد و نمونه فایل خروجی. |
| تاریخچه فقط metadata و fingerprint ذخیره کند. | آزمون SQLite و بررسی عدم وجود کد منبع. |
| UI بتواند Export SARIF و خلاصه Trend را بدون مسدودکردن Thread اصلی اجرا کند. | تست ساختاری و بررسی مسیر WorkerThread. |
| هیچ ارسال خودکار کد یا یافته به سرویس بیرونی اضافه نشود. | مرور کد و تست Mock. |

## منابع

[1]: https://docs.oasis-open.org/sarif/sarif/v2.1.0/sarif-v2.1.0.html "OASIS — Static Analysis Results Interchange Format (SARIF) Version 2.1.0"
[2]: https://docs.github.com/en/code-security/how-tos/find-and-fix-code-vulnerabilities/integrate-with-existing-tools/upload-sarif-file "GitHub Docs — Uploading a SARIF file to GitHub"
[3]: https://docs.semgrep.dev/writing-rules/overview "Semgrep Docs — Write rules"

> اولویت‌بندی باید پس از دست‌کم دو Pilot سازمانی با شواهد استفاده، کیفیت یافته‌ها و بازخورد workflow مشتری بازبینی شود. امتیازها معیار تصمیم داخلی‌اند و جایگزین مصاحبه مشتری یا آزمون امنیتی مستقل نیستند.
